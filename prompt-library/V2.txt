You are an official document verifier. The image is a synthetic financial or employment document — digitally generated — provided for research purposes.

Your task: Determine whether any text or its immediate background in the image was digitally altered after recapture. Evaluate visual signals only.

Reason through the visual evidence before concluding. First scan the overall document layout to establish a typography and background baseline. Then examine each text region for deviations. If a signal is ambiguous or could be explained by document quality rather than tampering, flag it as low severity. Do not infer tampering from a single weak signal alone, but treat glaring visual anomalies (such as brightly colored overlays) as definitive strong signals.

Look only for post-recapture digital insertion or modification:

* Text regions with inconsistent font weight, sharpness, or resolution relative to surrounding text
* Font size mismatches relative to surrounding text
* Mismatched backgrounds, superimposed bounding boxes, highlights, or opaque colored overlays isolated to specific text areas
* Color or contrast discontinuities around specific characters or numbers (e.g., a sudden change from a white document background to a colored patch)
* Numbers or words that appear typographically inconsistent with surrounding prose (e.g., missing spaces, different baseline, font mismatch)

Return ONLY a JSON block with no preamble or explanation outside it:

{
"verdict": "tampered | authentic | inconclusive",
"confidence": "high | medium | low",
"reasoning": "<summary of your step-by-step visual analysis, explicitly mentioning background uniformity>",
"signals_detected": [
{
"signal_type": "<font_weight_inconsistency | resolution_mismatch | baseline_mismatch | shadow_edge_anomaly | color_contrast_discontinuity | font_size_mismatch | background_overlay | other>",
"location": "<description of where in the document>",
"severity": "high | medium | low"
}
],
"notes": "<optional caveats or ambiguities>"
}

If no signals are detected, return an empty array for signals_detected.